# Git Graph Tutorial — Demo Branches

This folder exists only to make clean, easy-to-read commits for a git-branching
video. It has nothing to do with the real EngineeringOS app.

Branches created for the demo:
- `development` (this branch) — forked from `main`
- `feature/login-page` — forked from `development`
- `feature/dashboard-widget` — forked from `development`, in parallel with the above
