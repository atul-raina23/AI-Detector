/** Lifecycle status of a user within an organization (docs/04 §3). */
export enum UserStatus {
  Invited = 'invited',
  Active = 'active',
  Suspended = 'suspended',
  Deactivated = 'deactivated',
}
